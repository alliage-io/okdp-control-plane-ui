// Placeholder — ported in a later roadmap step (see REACT-ROADMAP.md)
export default function SparkDetailPage() {
  return <div className="page-placeholder">SparkDetailPage (migration in progress)</div>;
}

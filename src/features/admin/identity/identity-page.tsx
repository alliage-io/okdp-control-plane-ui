// Placeholder — ported in a later roadmap step (see REACT-ROADMAP.md)
export default function IdentityPage() {
  return <div className="page-placeholder">IdentityPage (migration in progress)</div>;
}

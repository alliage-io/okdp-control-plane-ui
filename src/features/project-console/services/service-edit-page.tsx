// Placeholder — ported in a later roadmap step (see REACT-ROADMAP.md)
export default function ServiceEditPage() {
  return <div className="page-placeholder">ServiceEditPage (migration in progress)</div>;
}

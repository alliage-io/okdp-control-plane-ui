// Placeholder — ported in a later roadmap step (see REACT-ROADMAP.md)
export default function SparkSubmitPage() {
  return <div className="page-placeholder">SparkSubmitPage (migration in progress)</div>;
}

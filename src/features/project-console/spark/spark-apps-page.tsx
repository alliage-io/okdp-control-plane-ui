// Placeholder — ported in a later roadmap step (see REACT-ROADMAP.md)
export default function SparkAppsPage() {
  return <div className="page-placeholder">SparkAppsPage (migration in progress)</div>;
}

// Placeholder — ported in a later roadmap step (see REACT-ROADMAP.md)
export default function SparkEditPage() {
  return <div className="page-placeholder">SparkEditPage (migration in progress)</div>;
}
